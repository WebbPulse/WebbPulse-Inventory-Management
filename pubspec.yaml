name: webbpulse_checkout
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  go_router: ^13.0.1
  firebase_core: ^2.24.2
  firebase_auth: ^4.17.5
  cloud_firestore: ^4.15.5
  provider: ^6.1.1
  firebase_ui_auth: ^1.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/codelab.png
    - assets/saber.gif